import { MarkBookmarkedDirective } from './mark-bookmarked.directive';

describe('MarkBookmarkedDirective', () => {
  it('should create an instance', () => {
    const directive = new MarkBookmarkedDirective();
    expect(directive).toBeTruthy();
  });
});
